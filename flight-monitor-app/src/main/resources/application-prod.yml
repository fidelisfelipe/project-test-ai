spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  jpa:
    show-sql: false
  h2:
    console:
      enabled: false

logging:
  level:
    com.flightmonitor: INFO
    root: WARN
